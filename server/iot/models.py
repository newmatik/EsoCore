# The IoT app provides device-facing API endpoints but does not define its own models.
# Device authentication uses devices.Device and its api_key field.
# Telemetry ingestion creates telemetry.TelemetryPacket and telemetry.TelemetryPoint records.
